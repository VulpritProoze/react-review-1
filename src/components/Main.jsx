function Main({ children }) {
    return(
        <main className="min-h-screen px-6 md:px-12 lg:px-32 py-10 lg:py-16 relative">
            {children}
        </main>
    );
}

export default Main;

